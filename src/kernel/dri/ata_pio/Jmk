init(ata_pio, ata_pio.a)

preset(freestanding)
preset(optimize)
preset(debug)
preset(32)
preset(warn)

CFLAGS += -I $(ROOT)/include/kernel

archetype(c)

OBJECTS = ata_pio.o

type(static_lib)

finish
